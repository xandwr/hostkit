[package]
name = "hostkit"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.53.1", features = ["rt", "sync", "macros", "time"] }
tokio-util = { version = "0.7.19", features = ["rt"] }
